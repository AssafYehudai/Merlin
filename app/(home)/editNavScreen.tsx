import EditScreen from "@/modules/home/screens/EditScreen";
import { SafeAreaView } from "react-native-safe-area-context";

export default function EditNavScreen() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <EditScreen />
        </SafeAreaView>
    );
}
